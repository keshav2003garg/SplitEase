import React from 'react';
import { View, Text, TouchableNativeFeedback } from 'react-native';
import { trigger } from "react-native-haptic-feedback";

export default function EmailSettings({ sheet }) {
    return (
        <View className='flex-1'>
            
        </View>
    )
}